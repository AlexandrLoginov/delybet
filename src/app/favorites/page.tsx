import { FavoritesView } from "@/components/favorites/favorites-view";

export const metadata = {
  title: "Избранное · DelyBet",
};

export default function FavoritesPage() {
  return <FavoritesView />;
}
